<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Smart Farming Dashboard</title>

  <!-- Bootstrap & Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    /* Navbar */
    .navbar {
      background: linear-gradient(90deg, #0c253f 0%, #34495e 100%) !important;
      box-shadow: 0 2px 10px rgba(0, 255, 255, 0.3);
    }
    .navbar-brand {
      font-weight: bold;
      color: #0ff !important;
      text-shadow: 0 0 8px #0ff, 0 0 15px #00f6ff;
    }
    .navbar-nav .nav-link {
      color: rgba(255, 255, 255, 0.8) !important;
      transition: all 0.3s ease;
    }
    .navbar-nav .nav-link:hover,
    .navbar-nav .nav-link.active {
      color: #fff !important;
      text-shadow: 0 0 6px #0ff;
    }

    /* Sidebar */
    .sidebar {
      background: linear-gradient(135deg, #0d1b58 0%, #484783 100%);
      min-height: 100vh;
      padding-top: 60px;
      position: fixed;
      top: 0;
      left: 0;
      width: 220px;
      z-index: 1000;
    }
    .sidebar .nav-link {
      color: rgba(255, 255, 255, 0.8);
      margin: 5px 10px;
      border-radius: 8px;
    }
    .sidebar .nav-link:hover {
      background: rgba(255, 255, 255, 0.1);
      color: #fff;
    }
    .sidebar .nav-link i {
      margin-right: 10px;
    }

    /* Content area */
    .content {
      margin-left: 220px;
      padding: 20px;
    }

    .dashboard-container {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      padding: 2rem;
      margin: 2rem auto;
      max-width: 1200px;
    }

    /* Metric Cards */
    .metric-card {
      background: white;
      border-radius: 15px;
      padding: 1.5rem;
      margin-bottom: 1.5rem;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      border-left: 4px solid;
      position: relative;
      overflow: hidden;
    }
    .metric-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
    }
    .metric-card.temp { border-left-color: #ff6b6b; }
    .metric-card.humidity { border-left-color: #4ecdc4; }
    .metric-card.network { border-left-color: #45b7d1; }
    .metric-card.light { border-left-color: #f9ca24; }
    .metric-card.oxygen { border-left-color: #6c5ce7; }
    .metric-card.ph { border-left-color: #fd79a8; }

    .metric-label {
      font-size: 0.8rem;
      color: #666;
      text-transform: uppercase;
      font-weight: 600;
      letter-spacing: 1px;
      margin-bottom: 0.5rem;
    }
    .metric-value {
      font-size: 2.2rem;
      font-weight: 700;
      color: #2c3e50;
      margin-bottom: 0.5rem;
    }
    .progress-bar-container {
      height: 6px;
      background: #ecf0f1;
      border-radius: 3px;
      margin-top: 1rem;
      overflow: hidden;
    }
    .progress-bar-fill {
      height: 100%;
      border-radius: 3px;
      transition: width 0.6s ease;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .sidebar {
        width: 180px;
      }
      .content {
        margin-left: 180px;
      }
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="#"><i class="fas fa-leaf"></i> Smart Farming</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
              data-bs-target="#navbarNav" aria-controls="navbarNav" 
              aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    </div>
  </nav>

  <!-- Sidebar -->
  <div class="sidebar d-flex flex-column p-3">
    <h5 class="text-white mb-4"><i class="fas fa-sliders-h"></i> Control Panel</h5>
    <ul class="nav flex-column">
      <li class="nav-item"><a href="#" class="nav-link"><i class="fas fa-thermometer-half"></i> Sensor Settings</a></li>
      <li class="nav-item"><a href="#" class="nav-link"><i class="fas fa-tint"></i> Irrigation</a></li>
      <li class="nav-item"><a href="#" class="nav-link"><i class="fas fa-sun"></i> Lighting</a></li>
      <li class="nav-item"><a href="#" class="nav-link"><i class="fas fa-wind"></i> Ventilation</a></li>
      <li class="nav-item"><a href="#" class="nav-link"><i class="fas fa-database"></i> Data Logs</a></li>
    </ul>
  </div>

  <!-- Content -->
  <div class="content">
    <div class="dashboard-container">
      <div class="header text-center mb-4">
        <h1 class="dashboard-title"><i class="fas fa-seedling"></i> Smart Farming Dashboard</h1>
        <p class="text-muted">Real-time monitoring of your agricultural environment</p>
      </div>

      <div class="row row-equal-height">
        <!-- contoh satu card -->
        <div class="col-lg-4 col-md-6">
          <div class="metric-card temp">
            <div class="metric-label"><i class="fas fa-temperature-high"></i> Suhu Udara</div>
            <div class="metric-value">25°C</div>
            <div class="progress-bar-container">
              <div class="progress-bar-fill" style="width: 62%; background: #ff6b6b;"></div>
            </div>
          </div>
        </div>
        <!-- copy sisanya sesuai dashboard sebelumnya -->
      </div>

      <div class="text-center mt-4">
        <p class="text-muted"><i class="fas fa-clock"></i> Last updated: <span id="lastUpdate"></span></p>
      </div>
    </div>
  </div>

  <!-- Script -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Update timestamp
    function updateTime() {
      const now = new Date();
      document.getElementById('lastUpdate').textContent = now.toLocaleString('id-ID', {
        year: 'numeric', month: 'long', day: 'numeric',
        hour: '2-digit', minute: '2-digit', second: '2-digit'
      });
    }
    updateTime();
    setInterval(updateTime, 1000);

    // Simulate data update
    function simulateDataUpdate() {
      const bars = document.querySelectorAll('.progress-bar-fill');
      bars.forEach(bar => {
        const w = parseFloat(bar.style.width);
        const variation = (Math.random() - 0.5) * 10;
        const newWidth = Math.max(5, Math.min(95, w + variation));
        bar.style.width = newWidth + '%';
      });
    }
    setInterval(simulateDataUpdate, 10000);
  </script>
</body>
</html>
